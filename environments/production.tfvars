# Production Environment Configuration

environment              = "production"
aws_region              = "us-east-1"
enable_bucket_versioning = true   # Data protection for production
lambda_log_level        = "WARN"  # Less verbose, only warnings and errors
