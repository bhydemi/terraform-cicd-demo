# Development Environment Configuration (Local)

environment              = "dev"
aws_region              = "us-east-1"
enable_bucket_versioning = false   # No versioning needed for dev
lambda_log_level        = "DEBUG"  # Maximum verbosity for local development
